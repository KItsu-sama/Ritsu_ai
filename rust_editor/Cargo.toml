# =========================================
# File: Cargo.toml
# =========================================
[features]
default = ["cpu-parallel"]
cpu-parallel = ["rayon"]
cuda = ["dep:cuda-driver"]
opencl = ["dep:ocl"]
vulkan = ["dep:vulkano"]
metal = ["dep:metal"]